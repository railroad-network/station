# Claude Code: local settings stay out; shared project config and CLAUDE.md are tracked
.claude/settings.local.json

# Rust
/target/
**/target/

# cargo-fuzz: generated corpus and crash artifacts (keep Cargo.toml + targets)
/fuzz/corpus/
/fuzz/artifacts/
/fuzz/coverage/
# A standalone Cargo.lock is generated for the detached fuzz workspace.
/fuzz/Cargo.lock

# macOS
.DS_Store

# Editor swap files
*.swp

# Local environment
.env

# Local AI-agent work tickets (not for humans; never committed)
/.tickets/